#edi
# This is a basic workflow to help you get started with Actions
name: Task3FixAccess
env:
  DEPLOY_VER: "VarWithSHA-${{github.sha}}"
  YEAR: "2022"
  azure_subscription_id: ${{ secrets.azure_subscription_id }} 
  azure_subscription_tenant_id: ${{ secrets.azure_subscription_tenant_id }}
  service_principal_appid: ${{ secrets.service_principal_appid }}
  service_principal_password: ${{ secrets.service_principal_password }}

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
    - name: Git clone my repo
      uses: actions/checkout@v1

    - name: printPwd
      run: pwd

    #- name: Install Terraform
    #  run: curl -fsSL https://apt.releases.hashicorp.com/gpg | sudo apt-key add -
      #sudo apt-add-repository "deb [arch=amd64] https://apt.releases.hashicorp.com $(lsb_release -cs) main"
      #sudo apt-get update && sudo apt-get install terraform

    - name: 
      run: |
            export ARM_SUBSCRIPTION_ID="$azure_subscription_id"
            export ARM_TENANT_ID="$azure_subscription_tenant_id"
            export ARM_CLIENT_ID="$service_principal_appid"
            export ARM_CLIENT_SECRET="$service_principal_password"
            . ~/.bashrc
            printenv | grep ^ARM*

    - name: printPwd
      run: cd /home/runner/work/EXADEL-DevOps-SandBox-June2022/EXADEL-DevOps-SandBox-June2022/Task3

    - name: print ls
      run: ls -la

    - name: terraform run
      run: |
            terraform init
            terraform plan
#            terraform apply -auto-approve

    #- name: az account show
    #  run: az account show



# A workflow run is made up of one or more jobs that can run sequentially or in parallel
#jobs:
#  # This workflow contains a single job called "build"
#  build:
#    # The type of runner that the job will run on
#    runs-on: ubuntu-latest
#
#    # Steps represent a sequence of tasks that will be executed as part of the job
#    steps:
#      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
#      - uses: actions/checkout@v3
#
#      # Runs a single command using the runners shell
#      - name: Run a one-line script
#        run: echo Hello, world!
#
#      # Runs a set of commands using the runners shell
#      - name: Run a multi-line script
#        run: |
#          echo Add other actions to build,
#          echo test, and deploy your project.
